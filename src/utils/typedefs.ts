import type { vec2, vec3, vec4 } from "gl-matrix";

/** flat array of {@link vec2} */
export type vec2v = number[] | Float32Array;

/** flat array of {@link vec3} */
export type vec3v = number[] | Float32Array;

/** flat array of {@link vec4} */
export type vec4v = number[] | Float32Array;
